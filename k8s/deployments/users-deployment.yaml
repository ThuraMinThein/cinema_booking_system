apiVersion: apps/v1
kind: Deployment
metadata:
  name: users
  namespace: cinema-booking
  labels:
    app: users
    version: "1.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: users
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: users
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: users
          image: ${DOCKERHUB_USERNAME}/cinema-users:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 50051
              name: grpc
          envFrom:
            - configMapRef:
                name: users-config
          env:
            - name: JWT_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: cinema-secrets
                  key: jwt-secret-key
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: cinema-secrets
                  key: users-db-password
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            tcpSocket:
              port: 50051
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 50051
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
